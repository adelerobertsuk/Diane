# Production masters (Diane)

App-ready skins only.

- `{Name}-idle.png` and `{Name}-rec.png` at 1410×3072 RGBA
- True alpha cassette window
- No annotations, no phone chrome

Cursor installs from here into Assets.xcassets.
